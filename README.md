# QR-Generator-
QR Generator Web App – Built using Python (FastAPI) with HTML, CSS, and JavaScript frontend. Enables users to generate QR codes for text, URLs, images, and documents through a clean, responsive interface. The backend handles uploads and creates QR codes in real time using Python libraries.
